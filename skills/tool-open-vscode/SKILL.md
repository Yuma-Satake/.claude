---
name: tool-open-vscode
description: 現在の作業ディレクトリをVSCodeで開く。作業がひと区切りついてユーザーに差分(diff)や変更内容を目視で確認してほしい時、ユーザーが「VSCodeで開いて」「diffを見て(見せて)」「変更を確認して」「エディタで開いて」などと言った時に積極的に使用する。コード修正やファイル編集が完了した直後、レビューを促すタイミングでも自発的に使ってよい。
---

# tool-open-vscode

変更内容が置かれているディレクトリをVSCodeで開き、ユーザーが自分の目で変更内容(diff)を確認できるようにする。

## 実行手順

1. 変更内容が置かれているディレクトリを対象に以下を実行する。通常は現在の作業ディレクトリだが、別のworktreeやサブディレクトリで作業していた場合はそのパスを明示的に指定する。

```bash
code <変更内容のあるディレクトリ>
```

2. `code` コマンドが見つからない(`command not found`)場合は、以下にフォールバックする。

```bash
open -a "Visual Studio Code" <変更内容のあるディレクトリ>
```

3. VSCodeを開いたことをユーザーに一言伝える。

## 注意点

- ディレクトリを開くだけで十分。特定のファイルやビューを指定して開く必要がある場合(diffビューの直接指定など)は、このスキルの範囲外なので都度対応する。
- `code` コマンドの有無は環境によって異なるため、フォールバックを省略しない。
- このスキルの目的は「ユーザーに差分を見せること」であり、「cwdを開くこと」ではない。worktree内で作業していた場合にメインのチェックアウトを開くと、ユーザーには差分が一切見えない。開いたディレクトリがcwdと異なる場合は、その旨を一言添える。
